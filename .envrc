# .envrc for direnv integration
# This file automatically loads the Nix development environment when you cd into this directory
#
# Setup:
# 1. Install direnv: https://direnv.net/
# 2. Add 'eval "$(direnv hook bash)"' to your ~/.bashrc (or similar for other shells)
# 3. Run 'direnv allow' in this directory
#
# Choose ONE of the following options:

# Option 1: Use flakes (recommended for modern Nix)
if has nix_direnv_version || has nix; then
  if has nix_direnv_version; then
    # Use nix-direnv for better caching
    use flake
  else
    # Fallback to regular nix flake
    use flake .
  fi
# Option 2: Use traditional nix-shell (uncomment if not using flakes)
# use nix
fi

# Optional: Set up layout for Python development
# layout python python3.11

# Optional: Add local bin to PATH (for pip install --user)
PATH_add ~/.local/bin
